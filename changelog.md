# Changelog (r2.0)
## r2.0
+ **Added support for FreeBSD**
+ Library structure changed
+ Frequently used functions are moved to [tools.py](metaerrors/tools.py "Open file")
+ The build system has been changed
+ Added a new script to .run (Clean + Build wheel + Install)
## r1.1
+ Fixed structure of package system *([setup.py](setup.py), [version.txt](version.txt))*
+ Added workflow *([release_build.yml](.github/workflows/release_build.yml))*

## r1.0
+ **Added base structure of lib**
+ Fixed an issue related to running Windows commands

## That is all
